__pycache__/
*.pyc
.ipynb_checkpoints/
create_notebook.py

# NOTE: model.pkl, scaler.pkl, kmeans.pkl, feature_cols.json,
# and the dataset CSV are now tracked in Git so Streamlit Cloud
# can access them at deployment.

# DVC local secrets (contains auth tokens — NEVER commit)
.dvc/config.local
.dvc/tmp/
.dvc/cache/

# MLflow tracking (local only)
mlflow.db
mlruns/

# OS / IDE
.DS_Store
.vscode/
*.swp
